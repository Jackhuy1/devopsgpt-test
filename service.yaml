apiVersion: v1
kind: Service
metadata:
  name: snake-game-service
  namespace: snake-game-namespace
  annotations:
    service.beta.kubernetes.io/aws-load-balancer-type: "nlb"
spec:
  selector:
    app: snake-game
  ports:
  - protocol: TCP
    port: 8080
    targetPort: 8080
  type: LoadBalancer
